# راهنمای فنی طراحی چالش‌ها (Challenge Authoring Guidelines)

این سند استانداردهای لازم برای طراحی و تحویل چالش‌های مسابقه CTFarsi را مشخص می‌کند. تمامی طراحان متعهد به رعایت کامل این اصول هستند.

## ۱. اصول بنیادین سناریوها
- **ضد هوش مصنوعی (Anti-AI by Design):** چالش‌ها نباید صرفا با کپی کردن پیام خطا یا سورس‌کد در مدل‌های زبانی مانند ChatGPT یا Claude یا ایجنت‌های کدنویسی حل شوند. منطق چالش باید چندمرحله‌ای، وابسته به جریان داده (Data Flow) نامتعارف و مبهم‌سازی خلاقانه باشد.
- **تنوع سطوح سختی:** سطوح Easy (دست‌گرمی و تشویقی)، Medium (نیازمند تسلط مفهومی) و Hard / Insane (نیازمند اکسپلویت نوآورانه).
- **عدم وابستگی به Guessing:** حل چالش باید بر مبنای منطق ریاضی، باینری یا وب باشد؛ از طراحی سناریوهای حدسی یا Rabbit Holeهای بی‌هدف خودداری شود.

## ۲. فرمت پرچم‌ها (Flag Format)
- الگوی استاندارد پرچم: `CTFarsi{...}`
- کاراکترهای داخل آکولاد: حروف انگلیسی کوچک و بزرگ، اعداد و آندرلاین.
- مثال: `CTFarsi{c0mmun1ty_b4ck3d_4nt1_41_h4ck1ng}`
- فلگ‌ها باید تصادفی و غیرقابل حدس باشند و هرگز در سورس‌کد کلاینت بدون رمزنگاری یا ابهام‌سازی قرار نگیرند.

## ۳. استاندارد کانتینرسازی (Dockerization)
تمام چالش‌های تحت شبکه یا داینامیک باید با Docker تحویل داده شوند:
1. **ایزولاسیون کامل:** اجرای پروسه روی کاربر غیر روت (`USER ctf` یا `nobody`).
2. **محدودیت منابع:** حافظه رم حداکثر ۲۵۶ مگابایت و CPU حداکثر ۵۰ درصد در هر نمونه کانتینر.
3. **متغیر محیطی پرچم:** فلگ نباید هاردکد باشد؛ باید از طریق متغیر محیطی `FLAG` یا فایل `/flag` با دسترسی `0400` خوانده شود.
4. **فایل داکر و اسکریپت راه‌اندازی:** ارائه `Dockerfile`، `docker-compose.yml` و در صورت نیاز اسکریپت راه‌اندازی `entrypoint.sh`.
5. **بررسی سلامت (Healthcheck):** تعریف `HEALTHCHECK` در داکر برای اطمینان از بالا بودن سرویس در زیرساخت مانیتورینگ.

## ۴. مستند تحلیلی و اکسپلویت (Writeup & Solver Script)
هر چالش ارسالی باید شامل یک دایرکتوری `solve/` باشد که شامل:
- فایل `WRITEUP.md`: توضیح گام‌به‌گام آسیب‌پذیری و مسیر منطقی حل.
- اسکریپت `solve.py` یا اکسپلویت تست‌شده که پرچم را در کمتر از ۱۰ ثانیه استخراج کند.

## ۵. تعهد محرمانگی
طراحان به هیچ وجه مجاز به افشای جزییات سناریوها، تکنیک‌های به کار رفته یا پرچم‌ها به هیچ فرد یا تیمی پیش از پایان رسمی مسابقه نیستند. هرگونه تخلف منجر به حذف دائمی از جامعه CTFarsi خواهد شد.
